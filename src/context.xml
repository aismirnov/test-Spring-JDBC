<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<!-- <property name="username" value="${jdbc.username}" /> <property name="password" 
			value="${jdbc.password}" /> -->
	</bean>

	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<bean id="employeeDaoBean" class="ru.iteco.trainings.springjdbc.EmployeeDaoBean">
		<property name="dataSource" ref="dataSource" />

		<property name="sqlQueryToCreateEmp">
			<value>
			<![CDATA[
insert into Employee (EMPNO, ENAME, JOB_TITLE) values (?, ?, ?)
					]]>
			</value>
		</property>
		
		<property name="sqlQueryToRetrieveEmp">
			<value>
			<![CDATA[
select EMPNO, ENAME, JOB_TITLE from Employee where EMPNO = ?
					]]>
			</value>
		</property>

		<property name="sqlQueryToUpdateEmp">
			<value>
			<![CDATA[
update Employee set EMPNO = ?, ENAME = ?, JOB_TITLE = ? where EMPNO = ?
					]]>
			</value>
		</property>
		
		<property name="sqlQueryToDeleteEmp">
			<value>
			<![CDATA[
delete from Employee where EMPNO = ?
					]]>
			</value>
		</property>
	</bean>

	<context:property-placeholder location="src\\jdbc.properties" />


</beans>